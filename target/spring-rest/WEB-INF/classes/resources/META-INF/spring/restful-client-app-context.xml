<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 5a. A restTemplate bean is declared using the RestTemplate class. The class uses Castor 
        to inject the property messageConverters with an instance of MarshallingHttpMessageConverter. 
        
    -->   
    <bean id="restTemplate" class="org.springframework.web.client.RestTemplate">
        <!-- 6d. The restTemplate bean contains  the  constructor argument with a 
               reference to the httpRequestFactory bean as injected.  
               We also need to support the injection of credentials, that is done by
                a simple CustomCredentialsProvider class. -->
        <constructor-arg ref="httpRequestFactory"/>
        <property name="messageConverters">
        <list>
                <bean class="org.springframework.http.converter.xml.MarshallingHttpMessageConverter">
                    <property name="marshaller" ref="castorMarshaller"/>
                    <property name="unmarshaller" ref="castorMarshaller"/>
                    <!-- 5b. This property is injected with an anonymous bean declaration of a MediaType
                    	 instance, indicating that the only supported media is XML.
                    	As a result, the client is always expecting XML as the return data format, 
                    	and Castor will help perform the conversion between POJO and XML.
                    -->
                    <property name="supportedMediaTypes">
                        <list>
                            <bean class="org.springframework.http.MediaType">
                                <constructor-arg index="0" value="application"/>
                                <constructor-arg index="1" value="xml"/>
                            </bean>
                        </list>
                    </property>
                </bean>
            </list>
        </property>
    </bean>

    <bean id="castorMarshaller"
          class="org.springframework.oxm.castor.CastorMarshaller">
        <property name="mappingLocation" value="classpath:META-INF/spring/oxm-mapping.xml"/>
    </bean>

    <bean id="httpRequestFactory"
          class="org.springframework.http.client.HttpComponentsClientHttpRequestFactory">
        <constructor-arg>
            <bean class="org.apache.http.impl.client.DefaultHttpClient">
                <property name="credentialsProvider">
                    <bean class="com.apress.prospring4.ch12.CustomCredentialsProvider">
                        <property name="credentials">
                            <bean class="org.apache.http.auth.UsernamePasswordCredentials">
                                <constructor-arg name="userName" value="remote"/>
                                <constructor-arg name="password" value="remote"/>
                            </bean>
                        </property>
                    </bean>
                </property>
            </bean>
        </constructor-arg>
    </bean>
</beans>
